import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Partner Gyms & Studios",
  description:
    "Find gyms and fitness studios in Nairobi stocking GiGi Energy — the zero-sugar energy drink for workouts.",
}

export default function GymsLayout({ children }: { children: React.ReactNode }) {
  return children
}
